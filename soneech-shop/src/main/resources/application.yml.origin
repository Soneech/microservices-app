server:
  port: 0

spring:
  datasource:
    driver-class-name:
    url: jdbc:
    username:
    password:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect:
        show_sql: true

  application:
    name: soneech-shop

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

app:
  messages:
    not-found:
      company: комания с таким id не найдена
      product: товар с таким id не найден
